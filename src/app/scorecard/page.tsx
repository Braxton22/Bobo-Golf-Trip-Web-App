import { ComingSoon } from "@/components/coming-soon";

export default function ScorecardPage() {
  return (
    <ComingSoon
      title="Scorecard"
      phase="Phase 6"
      blurb="Per-hole gross entry with offline queue. Net is computed automatically."
    />
  );
}
