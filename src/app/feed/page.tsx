import { ComingSoon } from "@/components/coming-soon";

export default function FeedPage() {
  return (
    <ComingSoon
      title="Activity Feed"
      phase="Phase 9"
      blurb="Birdies, match lead changes, decided matches, bets settled — live."
    />
  );
}
